# ChromaCraft-AI Worker Python Dependencies
# Gemini-only stack — Stability AI and rembg removed

# Core image processing
Pillow>=10.0.0
numpy>=1.24.0

# Google Gemini SDK (image + video generation)
google-genai>=1.0.0
